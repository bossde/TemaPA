#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define NUMBER_OF_TASKS 5

struct Player
{
    char* firstName;
    char* secondName;
    int points;
};

typedef struct Player Player;

struct Team{
    
}